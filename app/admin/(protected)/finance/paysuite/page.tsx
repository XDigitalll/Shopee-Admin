import { PaySuiteTransactionsView } from "@/components/admin/paysuite-transactions-view";

export const metadata = { title: "Transações PaySuite — Admin" };

export default function PaySuiteTransactionsPage() {
  return <PaySuiteTransactionsView />;
}
