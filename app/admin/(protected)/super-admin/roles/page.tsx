import { PlaceholderModule } from "@/components/admin/placeholder-module";

export default function RolesManagementPage() {
  return (
    <PlaceholderModule
      title="Roles & acessos"
      moduleName="Super Admin"
      description="O mapa de acessos do dashboard já reflete estas permissões em tempo real. Depois ligamos aqui a gestão detalhada."
    />
  );
}
