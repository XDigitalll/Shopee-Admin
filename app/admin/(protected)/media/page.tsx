import { MediaLibraryView } from "@/components/admin/media-library-view";

export const metadata = { title: "Multimédia — ShopeeMz Admin" };

export default function MediaPage() {
  return <MediaLibraryView />;
}
